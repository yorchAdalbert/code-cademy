/**
 * The describe and it functions accept two parameters: a descriptive string
 * and a callback function.
 */

const { describe, it } = require('mocha');

describe('Math', () => {
    describe('.min', () => {
        it('returns the argument with the lowest value', () => {

        })
    })
})